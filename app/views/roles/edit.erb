<form method="POST" action="/roles/<%= @role.id %>">
    <input type="hidden" name="_method" value="PATCH">

    Role Name: <input type="text" name="role[name]" value="<%=@role.name%>"><br><br>

    Select Movie:<br>
    <% @movies.each do |m| %>
        <input type="radio" name="role[movie_id]" value="<%= m.id %>" <% if @role.movie_id == m.id %>checked<% end %>><%= m.name %><br>
    <% end %><br>

    OR <br>Create a New Movie:<br>
    Movie Name: <input type="text" name="movie[name]"><br>
    Movie Description: <input type="text" name="movie[description]"><br><br>


    Select Actor: <br>
    <% @actors.each do |m| %>
        <input type="radio" name="role[actor_id]" value="<%= m.id %>" <% if m.id == @role.actor_id %>checked<% end %>><%= m.name %><br>
    <% end %><br>


    OR <br>Create a New Actor:<br>
    Actor Name: <input type="text" name="actor[name]"><br><br>

    <input type="submit" value="Create Role">
</form>